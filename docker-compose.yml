version: '3.8'

services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    # For development you may want to mount the source; comment out in production
    # volumes:
    #   - ./:/app
    restart: unless-stopped
